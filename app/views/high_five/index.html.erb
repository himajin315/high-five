<div class="inner cover">
  <h1 class="cover-heading">手相×恋愛相談</h1>
  <p class="lead">手相で恋愛相談相手を見つけよう。</p>
  <% if current_user %>
    <% if current_user.picture_url.nil? %>
      <p class="lead">
        <%= link_to '手相鑑定を依頼', entry_path, class:'btn btn-lg btn-secondary' %>
      </p>
    <% else %>
      <p class="lead">手相鑑定の結果までお待ち下さい。</p>
      <p class="lead">
        <%= image_tag current_user.picture.url %>
        <%= link_to '画像を変更する', entry_path, class:'btn btn-lg btn-secondary' %>
      </p>
    <% end %>
  <% else %>
    <p class="lead">
      <%= link_to 'Facebookでログイン', user_facebook_omniauth_authorize_path, class:'btn btn-lg btn-secondary' %>
    </p>
  <% end %>
</div>
